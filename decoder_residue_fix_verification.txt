RESIDUE FIX VERIFICATION — 2026-04-28
======================================
Commit verified: c0b0c92 — "Residue: fix sqlite3.Row .get() AttributeError, log silent failures"
Fix file: theory_x/stage6_fountain/generator.py


--- NEX5 RUNNING STATUS ---

PID 72688 confirmed running.
Process: /home/rr/Desktop/nex5/.venv/bin/python3 /home/rr/.local/bin/nex5
Started: 2026-04-28 22:57:18 (approximately 10 minutes before this verification)


--- RESIDUE TABLE ROW COUNTS ---

Before fix (pre-c0b0c92): 0 rows
  — The sqlite3.Row .get() AttributeError caused save_residue() to raise silently;
    the bare `except: pass` suppressed all errors, so no rows were ever written.

After fix (current state): 0 rows total, 0 consumed, 0 pending

IMPORTANT: The residue count is 0 not because the fix failed, but because
no fountain fires have occurred since the restart. The fountain runs on a
120-second interval with a readiness threshold, and the last fire was at
22:55:03 — before the restart at 22:57:18. Since restart, the fountain has
ticked (will_fire=False) at 22:59:18, 23:01:18, 23:02:18 but has not yet
fired. Residue rows are written on fire, so 0 is the expected count.

fountain_events total (all time): 2220
fountain_events since restart: 0
fountain_retrieval_log total: 0


--- SAMPLE RESIDUE ROWS WITH BELIEF CONTENT ---

No rows to show — residue table is empty (first post-fix fire has not yet occurred).
Residue table schema is confirmed correct:

  CREATE TABLE residue (
      id                 INTEGER PRIMARY KEY AUTOINCREMENT,
      cycle_id           TEXT NOT NULL,
      belief_id          INTEGER NOT NULL,
      activation_strength REAL NOT NULL,
      created_at         REAL NOT NULL,
      consumed_at        REAL,
      FOREIGN KEY (belief_id) REFERENCES beliefs(id)
  )


--- FOUNTAIN_RETRIEVAL_LOG RESIDUE SLOT ---

fountain_retrieval_log residue-slot rows: 0
All slots present: [] (table is empty — no fires since restart)

The fountain_retrieval_log was introduced in commit 41b3516
("fountain_retrieval_log: capture per-fire retrieval set"). It records which
beliefs were retrieved for each fire. With 0 fires post-restart, it is empty.
This is expected.


--- POP_RESIDUE CONSUMPTION STATUS ---

consumed_at population: N/A — no residue rows exist yet to consume.

The pop_residue code path (generator.py lines ~541-548) is invoked at the
start of _retrieve_context_beliefs() and marks consumed_at on up to 2
pending residue rows. This will be observable on the next fire.


--- RECENT 5 FIRES WITH RETRIEVAL MANIFESTS ---

No fires have occurred since restart. Most recent fires are pre-fix:

  fire_id=2668  ts=2026-04-28 22:55:03  (3m before restart)
    thought: "The quiet within the steady hum of my desktop fades into distant city lights."

  fire_id=2667  ts=2026-04-28 22:52:46
    thought: "[crypto.exchanges] {\"exchange\": \"kraken\", ...}"

  fire_id=2666  ts=2026-04-28 22:50:42
    thought: "The quiet within the steady hum of my desktop fades into distant city lights."

  fire_id=2665  ts=2026-04-28 22:48:22
    thought: "The quiet within the steady hum of my desktop fades into distant city lights."

  fire_id=2664  ts=2026-04-28 22:46:00
    thought: "The distant hum fades, and the warm sunlight casts gentle shadows across my desk"

Note: fountain_retrieval_log has 0 rows even for these pre-fix fires, because
the FRL table itself was only added in commit 41b3516 (before c0b0c92),
but those fires ran before that code was exercised post-restart. Pre-restart
fires are not retroactively logged.


--- ERRORS AND UNEXPECTED BEHAVIOR ---

1. DIVERSITY LOOP BUG (pre-existing, unrelated to residue fix):
   Every 60s since restart:
     "theory_x.diversity.loop WARNING Diversity loop tick failed:
      'sqlite3.Row' object has no attribute 'get'"
   This is the same sqlite3.Row .get() pattern that was fixed in c0b0c92
   for the fountain generator, but the diversity loop has the same bug
   in a different code path. This is a separate issue to fix.

2. Fountain will_fire=False on both ticks observed:
     22:59:18 — readiness=0.60, elapsed=1777409958.1s, will_fire=False
     23:01:18 — readiness=0.60, elapsed=1777410078.1s, will_fire=False
   The "elapsed" value (1.77B seconds) looks like an absolute epoch value
   being used as an interval, which is suspicious. However, this behavior
   predates the residue fix and may be an existing readiness guard. The
   fountain did fire 2220 times historically, so this is likely correct
   behavior (readiness gate, not a bug).

3. No errors attributable to the residue fix (c0b0c92). The logger.error
   handler for save_residue() has not fired (no "residue_save_failed" lines
   in the log), confirming no exceptions in the new code path.

4. Groove alert: "ngram_repetition" for 'i notice that' with severity=0.60.
   Cooldown written for +2h. This is expected diversity-guard behavior.


--- FIX CODE SUMMARY (c0b0c92) ---

Before: own_rows is a list of sqlite3.Row objects. Calling r.get("id")
raises AttributeError because sqlite3.Row has no .get() method.
The bare `except: pass` silently swallowed all these errors.
Result: residue table always stayed at 0 rows.

After: `own_rows_dicts = [dict(r) for r in own_rows]` converts all rows
to plain dicts before the loop. dict.get() works correctly. The except
now logs via logger.error() instead of silently passing.

The fix is correct and targeted. Residue writes will occur on the next
fountain fire where own_rows has more than own_n // 2 (= 3) candidates.


--- BELIEFS DB CONTEXT ---

Total beliefs: 987
Total residue rows: 0 (pending first post-fix fire)


Phase 1 complete — awaiting review before Phase 2 (feed window fix)
